[pytest]
testpaths = tests
python_files = test_*.py
addopts = -v --tb=short --cov=src --cov-report=term-missing
markers =
    unit: 单元测试（快速，隔离）
    integration: 集成测试（较慢，使用外部资源）
    slow: 慢速测试（例如真实API调用）